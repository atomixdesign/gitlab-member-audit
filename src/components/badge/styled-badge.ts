import styled from 'styled-components'

export const StyledBadge = styled.small`
  display: inline-block;
  border-radius: 3px;
  padding: 0.25em 0.5em;
  font-size: 0.5em;
  text-transform: uppercase;
  background: cornflowerblue;
  font-weight: bold;
  color: white;
`